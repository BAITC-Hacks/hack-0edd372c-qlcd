# Public base path INCLUDING the backend's /api prefix.
VITE_API_URL=/api
# Used only by the local Vite dev/preview proxy. /api is preserved.
API_PROXY_TARGET=http://127.0.0.1:8000
# Client waiting time; does not cancel server-side Python computation.
VITE_API_TIMEOUT_MS=10000
